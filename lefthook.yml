pre-commit:
  commands:
    check:
      run: npm format {staged_files}
      glob: "*.(js|jsx|ts|tsx|css|scss|md|json|yaml|yml)$"
      staged_fixed: true

pre-push:
  commands:
    validate:
      glob: "*.{tsx,ts}"
      run: npm validate:typecheck
